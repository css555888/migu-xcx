@import "./styles/iconfont.wxss";
/*   微信小程序中不支持通配符*   */
page,view,text,swiper-item,swiper,navigator{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* 主题颜色，通过变量来实现 */
page{
    /* 定义主题颜色 */
    --themeColor:#eb4450;
    /* 定义统一的字体大小 */
    font-size: 36rpx;
}